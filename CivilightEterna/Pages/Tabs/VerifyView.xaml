<UserControl x:Class="CivilightEterna.Pages.Tabs.VerifyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CivilightEterna.Pages.Tabs"
                          mc:Ignorable="d" xmlns:uiw="http://schemas.inkore.net/lib/ui/wpf"
xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
             xmlns:s="https://github.com/canton7/Stylet"
        d:DataContext="{d:DesignInstance local:SettingViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="1" Text="{Binding DisplayName, Converter={StaticResource StringSplitbySpaceConverter}, ConverterParameter=0}" FontFamily="{StaticResource FluentIcon}" FontSize="240" VerticalAlignment="Bottom" Foreground="#20000000" HorizontalAlignment="Right" Margin="0,0,-20,-20" />
        <Grid Grid.Row="0" Margin="20,0,20,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                <TextBlock Text="{Binding DisplayName, Converter={StaticResource StringSplitbySpaceConverter}, ConverterParameter=0}" FontSize="28"  FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" Foreground="{DynamicResource {x:Static ui:ThemeKeys.AccentTextFillColorPrimaryBrushKey}}"/>
                <TextBlock Text="{Binding DisplayName, Converter={StaticResource StringSplitbySpaceConverter}, ConverterParameter=2}" FontSize="28" FontWeight="Bold" VerticalAlignment="Center" Margin="8,0,0,0"/>
                <TextBlock Text="{Binding DisplayName, Converter={StaticResource StringSplitbySpaceConverter}, ConverterParameter=1}" VerticalAlignment="Bottom" Foreground="{DynamicResource {x:Static ui:ThemeKeys.TextFillColorTertiaryBrushKey}}" Margin="4,0,0,2"/>
            </StackPanel>
        </Grid>
        <ScrollViewer Grid.Row="1" Margin="20,10,3,0" Padding="0,0,17,20" >
            <uiw:SimpleStackPanel Spacing="10">
                <uiw:SimpleStackPanel>
                    <TextBlock Text="旧文件或文件夹" Margin="12,0,0,0" Foreground="{DynamicResource TextForeLabelGray}"/>
                    <Grid>
                        <TextBox d:Text="FILES" Text="{Binding OldFileName}"  MaxLines="1" FontWeight="Medium" Foreground="{DynamicResource TextForeContentGray}" Margin="0,0,190,0" />
                        <Button Command="{s:Action BrowseOldFile}" HorizontalAlignment="Right" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}" Width="80" Margin="0,0,100,0">
                            <uiw:SimpleStackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="&#xe8e5;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="16" />
                                <TextBlock Text="文件" VerticalAlignment="Center"/>
                            </uiw:SimpleStackPanel>
                        </Button>
                        <Button Command="{s:Action BrowseOldFile}" CommandParameter="TRUE" HorizontalAlignment="Right" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}">
                            <uiw:SimpleStackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="&#xe8de;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="16" />
                                <TextBlock Text="文件夹" VerticalAlignment="Center"/>
                            </uiw:SimpleStackPanel>
                        </Button>
                    </Grid>
                </uiw:SimpleStackPanel>
                <uiw:SimpleStackPanel>
                    <TextBlock Text="差分补丁" Margin="12,0,0,0" Foreground="{DynamicResource TextForeLabelGray}"/>
                    <Grid>
                        <TextBox d:Text="FILES" Text="{Binding DiffFileName}"  MaxLines="1" FontWeight="Medium" Foreground="{DynamicResource TextForeContentGray}" Margin="0,0,90,0" />
                        <Button Command="{s:Action BrowseDiffFile}" HorizontalAlignment="Right" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}" Width="80">
                            <uiw:SimpleStackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="&#xe8e5;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="16" />
                                <TextBlock Text="文件" VerticalAlignment="Center"/>
                            </uiw:SimpleStackPanel>
                        </Button>
                    </Grid>
                </uiw:SimpleStackPanel>
                <uiw:SimpleStackPanel>
                    <TextBlock Text="新文件或文件夹" Margin="12,0,0,0" Foreground="{DynamicResource TextForeLabelGray}"/>
                    <Grid>
                        <TextBox d:Text="FILES" Text="{Binding NewFileName}"  MaxLines="1" FontWeight="Medium" Foreground="{DynamicResource TextForeContentGray}" Margin="0,0,190,0" />
                        <Button Command="{s:Action SaveNewFile}" HorizontalAlignment="Right" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}" Width="80" Margin="0,0,100,0">
                            <uiw:SimpleStackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="&#xe8de;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="16" />
                                <TextBlock Text="文件" VerticalAlignment="Center"/>
                            </uiw:SimpleStackPanel>
                        </Button>
                        <Button Command="{s:Action SaveNewFile}" CommandParameter="False" HorizontalAlignment="Right" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}">
                            <uiw:SimpleStackPanel Orientation="Horizontal" Spacing="10">
                                <TextBlock Text="&#xe8de;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="16" />
                                <TextBlock Text="文件夹" VerticalAlignment="Center"/>
                            </uiw:SimpleStackPanel>
                        </Button>
                    </Grid>
                </uiw:SimpleStackPanel>
                <Expander Padding="10,8,10,8" Visibility="{Binding SpecItems.Count, Converter={StaticResource CollectionToVisiConverter}, Mode=OneWay}">
                    <Expander.Background>
                        <SolidColorBrush Color="{DynamicResource MAINDARKO1}"/>
                    </Expander.Background>
                    <Expander.Header>
                        <uiw:SimpleStackPanel Orientation="Horizontal" Margin="5,0,12,0" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Left">
                            <TextBlock Text="&#xe713;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="18" />
                            <TextBlock Text="配置" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0,0,0"></TextBlock>
                            <TextBlock Text="hdiff v4.12" VerticalAlignment="Center" Margin="5,0,0,0" Foreground="{DynamicResource TextForeLabelGray}"/>
                        </uiw:SimpleStackPanel>
                    </Expander.Header>
                    <Border Background="White" CornerRadius="3" Padding="10">
                        <uiw:SimpleStackPanel Spacing="10">
                            <uiw:SimpleStackPanel>
                                <TextBlock Text="内存限制（MB）" Margin="12,0,0,0" Foreground="{DynamicResource TextForeLabelGray}"/>
                                <TextBox d:Text="FILES" InputScope="Number" Text="{Binding Memory}"  MaxLines="1" FontWeight="Medium" Foreground="{DynamicResource TextForeContentGray}" />
                            </uiw:SimpleStackPanel>
                        </uiw:SimpleStackPanel>
                    </Border>
                </Expander>

                <Button IsEnabled="{Binding IsRunning, Converter={StaticResource BooleanInverseConverter}}" Command="{s:Action ProcessStart}" HorizontalAlignment="Stretch" Background="{DynamicResource TASUCCESS}" Style="{StaticResource {x:Static ui:ThemeKeys.AccentButtonStyleKey}}">
                    <FrameworkElement.Resources>
                        <SolidColorBrush x:Key="{x:Static ui:ThemeKeys.AccentButtonBackgroundPointerOverKey}" Color="#FF076A02" Opacity=".8"/>
                        <SolidColorBrush x:Key="{x:Static ui:ThemeKeys.AccentButtonBackgroundPressedKey}" Color="#FF084B05"/>
                    </FrameworkElement.Resources>
                    <uiw:SimpleStackPanel Orientation="Horizontal" >
                        <ui:ProgressRing Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisiCollaConverter}}" IsIndeterminate="{Binding IsRunning}" Width="16" Height="16" d:Value="50" Foreground="White" Margin="0,0,10,0" />

                        <TextBlock Text="&#xe8c8;" FontFamily="{StaticResource FluentIcon}" VerticalAlignment="Center" FontSize="16" Margin="0,0,10,0" />
                        <TextBlock Text="正在" Visibility="{Binding IsRunning, Converter={StaticResource BoolToVisiCollaConverter}}" VerticalAlignment="Center"/>
                        <TextBlock Text="校验" VerticalAlignment="Center"/>
                    </uiw:SimpleStackPanel>
                </Button>

            </uiw:SimpleStackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
